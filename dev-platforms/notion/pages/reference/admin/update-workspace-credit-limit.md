---
title: "Update the default agent credit limit for a workspace"
source: https://developers.notion.com/reference/admin/update-workspace-credit-limit
path: reference/admin/update-workspace-credit-limit
---

openapi-adminApi.json PATCH /v1/spaces/{space_id}/credit_limit
Update the default agent credit limit for a workspace.

The organization bot token must have the following scopes:

* `workflows:write`
