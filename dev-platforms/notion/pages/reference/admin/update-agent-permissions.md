---
title: "Update a custom agent's sharing permissions"
source: https://developers.notion.com/reference/admin/update-agent-permissions
path: reference/admin/update-agent-permissions
---

openapi-adminApi.json PATCH /v1/spaces/{space_id}/agents/{agent_id}/permissions
Update a custom agent's sharing permissions.

The organization bot token must have the following scopes:

* `workflows:write`
